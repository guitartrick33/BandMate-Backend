# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

#
#name: Java CI with Maven & Deployment
#
#on:
#  push:
#    branches: [ main ]
#  pull_request:
#    branches: [ main ]
#
#permissions:
#  contents: read
#
#jobs:
#  build:
#    name: Build
#    runs-on: ubuntu-latest
#
#    steps:
#      - uses: actions/checkout@v2
#      - name: Set up JDK 11
#        uses: actions/setup-java@v2
#        with:
#          java-version: '11'
#          distribution: 'temurin'
#          cache: maven
#      - name: Build with Maven
#        run: mvn -B package --file pom.xml -Dmaven.test.skip=true
#
#  - name: Auth
#      id: 'auth'
#      uses: 'google-github-actions/auth@v0'
#      with:
#        credentials_json: '${{ secrets.GCP_KEY }}'
#        create_credentials_file: true
#
#      # Setting up tools for iteracting with google cloud services
#      - name: 'Set up Cloud SDK'
#        uses: 'google-github-actions/setup-gcloud@v0'
#
#      #Chech google cloud connection
#      - name: 'Use gcloud CLI'
#        run: 'gcloud info'
#
#      - name: Build and push to Artifact registry
#        working-directory: bandmate-auth
#        run: |
#          gcloud auth login --brief --cred-file=${{ steps.auth.outputs.credentials_file_path }}
#          gcloud auth configure-docker europe-west4-docker.pkg.dev
#          docker build -t europe-west4-docker.pkg.dev/bandmate-backend/bandmate-auth/bandmate-auth:latest .
#          docker push europe-west4-docker.pkg.dev/bandmate-backend/bandmate-auth/bandmate-auth:latest


name: Build and Deploy to GKE

on:
  push:
    branches:
      - main

env:
  PROJECT_ID: bandmate-backend
  GKE_CLUSTER: bandmate-auth    # Add your cluster name here.
  GKE_ZONE: eu-west4-a   # Add your cluster zone here.
  DEPLOYMENT_NAME: bandmate-auth # Add your deployment name here.
  IMAGE: bandmate-auth/bandmate-auth

jobs:
  setup-build-publish-deploy:
    name: Setup, Build, Publish, and Deploy
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v3
          
          # Authentication to google cloud services
                - name: Auth
                id: 'auth'
                uses: 'google-github-actions/auth@v0'
                with:
                  credentials_json: 'a74cb9c8155220d813cd2110f40639248caadcd6'
                  create_credentials_file: true

          # Setting up tools for iteracting with google cloud services
                - name: 'Set up Cloud SDK'
                  uses: 'google-github-actions/setup-gcloud@v0'

          #Chech google cloud connection
                - name: 'Use gcloud CLI'
                  run: 'gcloud info'

                - name: Auth to google cloud services
                  run: |
                    gcloud auth login --brief --cred-file=${{ steps.auth.outputs.credentials_file_path }}
                    gcloud auth configure-docker europe-west4-docker.pkg.dev

                - name: Build IMAGE
                  run:
                    docker build -t europe-west4-docker.pkg.dev/bandmate-backend/bandmate-auth/bandmate-auth:latest .

                - name: Push image to registry
                  run:
                    docker push europe-west4-docker.pkg.dev/bandmate-backend/bandmate-auth/bandmate-auth:latest

          # Set up kustomize
                - name: Set up Kustomize
                  run: |-
                    curl -sfLo kustomize https://github.com/kubernetes-sigs/kustomize/releases/download/v3.1.0/kustomize_3.1.0_linux_amd64
                    chmod u+x ./kustomize
          # Deploy the Docker image to the GKE cluster
                - name: Deploy
                  run: |-
                    ./kustomize edit set image gcr.io/PROJECT_ID/IMAGE:TAG=gcr.io/bandmate-backend/bandmate-auth:latest
                    ./kustomize build . | kubectl apply -f -
                    kubectl rollout status deployment/bandmate-auth
                    kubectl get services -o wide